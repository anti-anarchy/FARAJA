# Responders app env — copy to .env.local
# Django RAPIDA backend base — SAME backend the dashboard uses.
# When set, the app reads live reports/assignments instead of mock data.
RAPIDA_API_BASE=http://localhost:8000/api
